/** @clearkrypt/cli — implementation lands with the CLI milestone. */
export async function main(_args: string[]): Promise<number> {
  console.error('clearkrypt: not yet implemented');
  return 64;
}
