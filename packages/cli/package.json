{
  "name": "clearkrypt",
  "version": "0.1.0",
  "description": "ClearKrypt command-line interface: new, check, build, emit, format, and language-server.",
  "license": "MIT",
  "homepage": "https://github.com/gitbub0816/clearkrypt_ide",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "bin"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc --build",
    "bundle": "node scripts/bundle.mjs",
    "prepack": "node scripts/bundle.mjs"
  },
  "bin": {
    "clearkrypt": "./bin/clearkrypt.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "@clearkrypt/compiler-core": "0.1.0",
    "@clearkrypt/emitter-swift": "0.1.0",
    "@clearkrypt/emitter-kotlin": "0.1.0",
    "@clearkrypt/emitter-react": "0.1.0",
    "@clearkrypt/formatter": "0.1.0",
    "@clearkrypt/language-service": "0.1.0"
  }
}
