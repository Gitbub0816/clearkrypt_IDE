# @clearkrypt/ide-native

Reserved package for the native desktop IDE shell: window shell, project
open flow, file tree, editor host, diagnostics panel, and build runner.

Per the roadmap, the IDE shell (Milestone 9) is built only after the
compiler spine and language service exist. This folder marks the package
boundary; it intentionally contains no code yet.
